#info:
#  component: Atlas1

spring:
  cloud:
    config:
      enabled: false


consul:
  host: localhost
  port: 8500
  enabled: true
  tags:
    - production
#  managementTags: management
  heartbeat:
    ttlValue: 9
    fixedRate: 8000
  serviceOverride:
    address: localhost
